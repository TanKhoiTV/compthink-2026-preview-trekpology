# Copy thành .env rồi điền connection string Postgres thật (KHÔNG commit .env).
# Thiếu biến này: server vẫn chạy nhưng không lưu tài khoản/lịch sử.
DATABASE_URL=postgres://user:password@host:5432/trekkopoly_db

# Bí mật ký token đăng nhập (đổi khi deploy production).
AUTH_SECRET=change-me-please
